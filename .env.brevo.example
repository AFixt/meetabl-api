# Brevo (SendinBlue) SMTP Configuration
# Add these to your .env file with your actual Brevo credentials

# For Brevo SMTP
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_USER=YOUR_BREVO_LOGIN_EMAIL
EMAIL_PASS=YOUR_BREVO_SMTP_KEY

# Email sender information
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=Meetabl

# Alternative names (the app supports both)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=YOUR_BREVO_LOGIN_EMAIL
SMTP_PASS=YOUR_BREVO_SMTP_KEY
SMTP_FROM=noreply@yourdomain.com

# To get your Brevo SMTP credentials:
# 1. Log in to your Brevo account
# 2. Go to SMTP & API section
# 3. Create an SMTP key if you haven't already
# 4. Use your login email as EMAIL_USER/SMTP_USER
# 5. Use the SMTP key as EMAIL_PASS/SMTP_PASS